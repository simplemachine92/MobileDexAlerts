function solution(area) {
	let area = 20;
	let counter = 0;
	if (area >= 1, area <= 1000000) {
	if (Math.floor(Math.sqrt(i)) ** 2 === i) { 
                console.log(Math.floor(Math.sqrt(i))** 2)
	}
}
